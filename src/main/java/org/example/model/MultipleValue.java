package org.example.model;

import java.util.List;

public class MultipleValue extends Value{
    public MultipleValue(List<String> patterns, boolean selectionType){
        super(patterns, selectionType);
    }
}
